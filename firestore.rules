rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // NOTE: These rules are relaxed for DEMO PURPOSES to allow the app to work 
    // even if Firebase Authentication is not fully configured.
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
